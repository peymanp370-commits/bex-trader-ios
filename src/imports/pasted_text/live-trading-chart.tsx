Fix the Chart screen and rebuild it as a real professional live trading chart similar to MetaTrader or TradingView behavior. Base URL: https://bex-app.peymanp370.workers.dev PRIMARY GOAL: This must be a REAL LIVE ACTIVE CANDLE CHART, not a fake UI block. The current chart is not acceptable. It must behave like a professional trading chart with: * live candles * exact time labels * zoom in * zoom out * horizontal pan * responsive redraw * professional candle rendering * real OHLC data * real time axis DO NOT create a static placeholder chart. DO NOT create fake candles. DO NOT use decorative rectangles instead of candles. DO NOT use random timestamps. DO NOT use approximate time labels. DO NOT hardcode values. USE LIVE API DATA ONLY. ===================================== DATA SOURCE =========== Use this endpoint for chart candles: GET /api/chart?symbol={SYMBOL}&tf={TIMEFRAME}&limit=200 Examples: * /api/chart?symbol=XAUUSD&tf=1m&limit=200 * /api/chart?symbol=XAUUSD&tf=5m&limit=200 * /api/chart?symbol=XAUUSD&tf=15m&limit=200 * /api/chart?symbol=XAUUSD&tf=1h&limit=200 * /api/chart?symbol=XAGUSD&tf=1m&limit=200 * /api/chart?symbol=XAGUSD&tf=5m&limit=200 * /api/chart?symbol=XAGUSD&tf=15m&limit=200 * /api/chart?symbol=XAGUSD&tf=1h&limit=200 Supported symbols: * XAUUSD * XAGUSD Supported live backend timeframes: * 1m * 5m * 15m * 1h For H4 and D1: * do not generate fake candles * if real API support is unavailable, derive them only from real 1h candles * H4 = aggregate 4 consecutive H1 candles * D1 = aggregate 24 consecutive H1 candles * open = first open * high = highest high * low = lowest low * close = last close * volume = summed volume ===================================== EXPECTED RESPONSE FORMAT ======================== The API candle response can look like this: { "ok": true, "symbol": "XAUUSD", "tf": "15m", "count": 120, "candles": [ { "symbol": "XAUUSD", "bucket": 1710000000000, "open": 3025.10, "high": 3028.40, "low": 3022.80, "close": 3027.90, "volume": 0, "isoTime": "2025-03-29T12:00:00.000Z" } ] } Map fields like this: * time = bucket * open = open * high = high * low = low * close = close * volume = volume If bucket is in milliseconds, convert it correctly to chart time. Do not display invalid timestamps. ===================================== CHART REQUIREMENTS ================== Build a real candlestick chart with: * price scale on the right side * time scale on the bottom * exact candle times on bottom axis * active crosshair * zoom in support * zoom out support * drag / pan left and right * auto fit content on first load * live update behavior * touch support on mobile * professional redraw quality The chart must feel like MetaTrader / TradingView: * smooth * interactive * precise * dense professional candle spacing * correct axis scaling * readable time labels * readable price labels Use a real charting component, not custom fake blocks. Use a proper candlestick chart library behavior. ===================================== TIME AXIS RULES =============== The bottom axis must show exact real times. This is mandatory. Examples: * for 1m: show minute-level labels like 09:31, 09:32, 09:33 * for 5m: show proper 5-minute spacing * for 15m: show proper 15-minute spacing * for 1h: show proper hourly labels * for H4 and D1: show correct larger time intervals Do not show vague text. Do not show repeated labels. Do not show broken time formatting. Do not use placeholder time strings. Use local readable formatting consistently. If needed, use the candle isoTime or bucket timestamp to build exact bottom labels. ===================================== LIVE UPDATE RULES ================= The chart must actively refresh and feel live. Rules: * reload latest candles automatically every 10 to 15 seconds * preserve current symbol and timeframe * do not reset zoom on every refresh * do not jump back to default position if user has panned left * if user is viewing the latest candles, keep the chart following live candles * if user has moved away from the latest candles, do not force auto-jump * update only with real API data Keep the latest valid chart visible while refreshing. ===================================== SYMBOL AND TIMEFRAME BEHAVIOR ============================= Top tabs: * XAUUSD * XAGUSD Timeframe tabs: * M1 * M5 * M15 * H1 * H4 * D1 Behavior: * default symbol = XAUUSD * default timeframe = M15 * switching symbol reloads chart * switching timeframe reloads chart * selected tab must stay visibly active * chart must redraw immediately after changing symbol or timeframe Mapping: * M1 = 1m * M5 = 5m * M15 = 15m * H1 = 1h * H4 = derived from 1h if native not available * D1 = derived from 1h if native not available ===================================== VISUAL STYLE ============ Style the chart as a premium dark professional metals trading terminal. Requirements: * dark background * subtle grid * green bullish candles * red bearish candles * thin but readable wick lines * clear candle bodies * soft axis colors * strong contrast for active data * compact professional layout * no oversized empty area * no fake panel look The chart area should dominate the screen visually. Reduce empty padding. Make it look like an actual trading terminal. ===================================== OPTIONAL SIGNAL OVERLAY ======================= If signal data is available from: GET /api/signal?symbol={SYMBOL} Then overlay: * Entry line * SL line * TP line * BUY or SELL label But only if those fields are truly available from API. Do not invent overlay levels. ===================================== ERROR AND LOADING RULES ======================= * While loading, show a real chart loading state * If fetch fails, show: Chart data unavailable * Do not blank the full screen * Keep last successful chart if available * Never replace missing data with fake candles ===================================== IMPORTANT IMPLEMENTATION RULE ============================= Frontend must only call: https://bex-app.peymanp370.workers.dev Do not call internal workers directly. Do not use private bindings. Do not create demo chart data. This screen must function as a real live professional candlestick chart, not a mockup.