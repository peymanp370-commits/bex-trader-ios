{
  "name": "bex-push-worker",
  "private": true,
  "type": "module",
  "dependencies": {
    "express": "^5.2.1",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "vapid": "web-push generate-vapid-keys"
  }
}
